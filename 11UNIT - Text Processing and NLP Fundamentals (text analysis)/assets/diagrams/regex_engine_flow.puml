@startuml regex_engine_flow
!theme plain
skinparam backgroundColor #1a1a2e
skinparam defaultFontColor #eaeaea
skinparam ArrowColor #4a9eff
skinparam ActivityBackgroundColor #2d2d44
skinparam ActivityBorderColor #4a9eff

title Regex Engine Processing Flow

start

:Receive Pattern and Text;

:Compile Pattern to NFA;
note right
  Non-deterministic
  Finite Automaton
end note

:Initialise Match Position;

while (More positions to try?) is (yes)
  :Try match at current position;
  
  if (Pattern matches?) then (yes)
    :Record match;
    :Return Match Object;
    stop
  else (no)
    :Advance position;
  endif
endwhile (no)

:Return None (no match);

stop

@enduml
