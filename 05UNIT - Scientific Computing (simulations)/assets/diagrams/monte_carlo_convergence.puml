@startuml monte_carlo_convergence
!theme plain
skinparam backgroundColor #1a1a2e
skinparam defaultFontColor #e0e0e0
skinparam ArrowColor #58a6ff

title Monte Carlo Integration Convergence

rectangle "Sample Generation" as SG #16213e {
  :Generate n uniform samples;
  :X_i ~ Uniform(a,b);
}

rectangle "Function Evaluation" as FE #16213e {
  :Evaluate f(X_i);
  :Store values;
}

rectangle "Estimation" as EST #16213e {
  :Compute mean;
  :I_n = (b-a)/n * Σf(X_i);
}

rectangle "Error Analysis" as EA #16213e {
  :Standard error;
  :SE = σ/√n;
  :95% CI;
}

SG --> FE : samples
FE --> EST : evaluations
EST --> EA : estimate

note right of EA
  Convergence Rate:
  O(1/√n)
  
  Dimension-independent!
end note

@enduml
