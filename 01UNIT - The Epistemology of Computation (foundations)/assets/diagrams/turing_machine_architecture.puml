@startuml turing_machine_architecture
!theme plain
skinparam backgroundColor #1a1a2e
skinparam defaultFontColor white
skinparam componentBorderColor #58a6ff
skinparam componentBackgroundColor #16213e
skinparam arrowColor #58a6ff
skinparam noteBackgroundColor #0f3460
skinparam noteBorderColor #58a6ff

title Turing Machine Architecture

package "Turing Machine M = (Q, Σ, Γ, δ, q₀, q_accept, q_reject)" {
    
    component "Finite Control" as FC #16213e {
        component "State Register" as SR
        component "Transition Function δ" as TF
        SR --> TF : current state q
    }
    
    component "Infinite Tape" as TAPE #0f3460 {
        rectangle "... | □ | □ | 1 | 0 | 1 | 1 | 0 | □ | □ | ..." as cells
    }
    
    component "Read/Write Head" as HEAD #3d2914

}

FC --> HEAD : control signals
HEAD --> TAPE : read symbol
HEAD <-- TAPE : write symbol
TF --> HEAD : move direction (L/R)

note right of FC
  <b>State Set Q</b>
  Finite set of internal states
  including q_accept and q_reject
end note

note bottom of TAPE
  <b>Tape Alphabet Γ</b>
  Includes input alphabet Σ
  and blank symbol □
end note

note left of TF
  <b>Transition Function</b>
  δ: Q × Γ → Q × Γ × {L, R}
  δ(q, a) = (q', b, D)
end note

legend right
  | Symbol | Meaning |
  | Q | State set |
  | Σ | Input alphabet |
  | Γ | Tape alphabet |
  | δ | Transition function |
  | q₀ | Initial state |
  | L/R | Left/Right movement |
endlegend

@enduml
